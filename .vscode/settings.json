{
  // The uv-managed venv: makes the ▷ "Run Python File" button, the debugger,
  // and IntelliSense all resolve `from lib import ...` / `from grader import ...`
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "python.terminal.activateEnvironment": true
}
